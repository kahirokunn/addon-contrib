# Code generated by hack/karpenter-sync; DO NOT EDIT.
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
{{- if eq .InstallMode "Hosted" }}
    addon.open-cluster-management.io/hosted-manifest-location: hosting
{{- end }}
    app.kubernetes.io/instance: '{{ .DeploymentName }}'
    app.kubernetes.io/managed-by: karpenter-provider-aws-addon
    app.kubernetes.io/name: '{{ .AddonName }}'
  name: '{{ .DeploymentName }}'
  namespace: '{{ .AddonInstallNamespace }}'
spec:
  replicas: 2
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app.kubernetes.io/instance: '{{ .DeploymentName }}'
      app.kubernetes.io/name: '{{ .AddonName }}'
  strategy:
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: '{{ .DeploymentName }}'
        app.kubernetes.io/managed-by: karpenter-provider-aws-addon
        app.kubernetes.io/name: '{{ .AddonName }}'
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: karpenter.sh/nodepool
                    operator: DoesNotExist
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app.kubernetes.io/instance: '{{ .DeploymentName }}'
                  app.kubernetes.io/name: '{{ .AddonName }}'
              topologyKey: kubernetes.io/hostname
      automountServiceAccountToken: true
{{- if eq .InstallMode "Hosted" }}
      volumes:
        - name: {{ .AWSIRSAStateVolumeName }}
          emptyDir: {}
        - name: {{ .ManagedKubeconfigVolumeName }}
          secret:
            secretName: {{ .ManagedKubeConfigSecret }}
      initContainers:
        - name: {{ .IRSASidecarContainerName }}
          image: {{ .IRSASidecarImage }}
          imagePullPolicy: IfNotPresent
          restartPolicy: Always
          command:
            - /aws-irsa-sidecar
          args:
            - --kubeconfig={{ .ManagedKubeconfigPath }}
            - --token-file={{ .AWSTokenPath }}
            - --aws-config-file={{ .AWSConfigPath }}
          startupProbe:
            exec:
              command:
                - /aws-irsa-sidecar
                - check
                - --token-file={{ .AWSTokenPath }}
                - --aws-config-file={{ .AWSConfigPath }}
            periodSeconds: 2
            failureThreshold: 90
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: {{ .AWSIRSAStateVolumeName }}
              mountPath: /var/run/aws-irsa
            - name: {{ .ManagedKubeconfigVolumeName }}
              mountPath: /managed/config
              readOnly: true
{{- end }}
      containers:
        - env:
{{- if eq .InstallMode "Hosted" }}
            - name: KUBECONFIG
              value: {{ .ManagedKubeconfigPath }}
            - name: AWS_CONFIG_FILE
              value: {{ .AWSConfigPath }}
            - name: AWS_SDK_LOAD_CONFIG
              value: "1"
{{- end }}
            - name: KUBERNETES_MIN_VERSION
              value: 1.19.0-0
            - name: KARPENTER_SERVICE
              value: '{{ .DeploymentName }}'
            - name: LOG_LEVEL
              value: info
            - name: LOG_OUTPUT_PATHS
              value: stdout
            - name: LOG_ERROR_OUTPUT_PATHS
              value: stderr
            - name: METRICS_PORT
              value: "8080"
            - name: HEALTH_PROBE_PORT
              value: "8081"
            - name: SYSTEM_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: CPU_REQUESTS
              valueFrom:
                resourceFieldRef:
                  containerName: '{{ .KarpenterContainerName }}'
                  divisor: 1m
                  resource: requests.cpu
            - name: MEMORY_LIMIT
              valueFrom:
                resourceFieldRef:
                  containerName: '{{ .KarpenterContainerName }}'
                  divisor: "0"
                  resource: limits.memory
            - name: FEATURE_GATES
              value: ReservedCapacity=true,SpotToSpotConsolidation=false,NodeRepair=false,NodeOverlay=false,StaticCapacity=false
            - name: BATCH_MAX_DURATION
              value: 10s
            - name: BATCH_IDLE_DURATION
              value: 1s
            - name: PREFERENCE_POLICY
              value: Respect
            - name: MIN_VALUES_POLICY
              value: Strict
            - name: AWS_REGION
              value: '{{ .AWSRegion }}'
            - name: AWS_DEFAULT_REGION
              value: '{{ .AWSRegion }}'
            - name: CLUSTER_NAME
              value: '{{ .KarpenterClusterName }}'
            - name: CLUSTER_ENDPOINT
              value: '{{ .ClusterEndpoint }}'
            - name: VM_MEMORY_OVERHEAD_PERCENT
              value: "0.075"
{{- if .InterruptionQueue }}
            - name: INTERRUPTION_QUEUE
              value: '{{ .InterruptionQueue }}'
{{- end }}
            - name: RESERVED_ENIS
              value: "0"
            - name: IGNORE_DRA_REQUESTS
              value: "true"
          image: '{{ .KarpenterImage }}'
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 30
            timeoutSeconds: 30
          name: '{{ .KarpenterContainerName }}'
{{- if eq .InstallMode "Hosted" }}
          volumeMounts:
            - name: {{ .AWSIRSAStateVolumeName }}
              mountPath: /var/run/aws-irsa
              readOnly: true
            - name: {{ .ManagedKubeconfigVolumeName }}
              mountPath: /managed/config
              readOnly: true
{{- end }}
          ports:
            - containerPort: 8080
              name: http-metrics
              protocol: TCP
            - containerPort: 8081
              name: http
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            timeoutSeconds: 30
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 65532
            runAsNonRoot: true
            runAsUser: 65532
      dnsPolicy: ClusterFirst
      nodeSelector:
        kubernetes.io/os: linux
      priorityClassName: system-cluster-critical
      schedulerName: default-scheduler
      securityContext:
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: '{{ .ServiceAccountName }}'
      tolerations:
        - key: CriticalAddonsOnly
          operator: Exists
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app.kubernetes.io/instance: '{{ .DeploymentName }}'
              app.kubernetes.io/name: '{{ .AddonName }}'
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
